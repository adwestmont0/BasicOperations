# BasicOperations
Create a Java Program that will initialize two positive integer numbers to compute and display the product without the multiplication operator, display the integer quotient without using the division operator, and display the integer remainder without using the modulo operator.
